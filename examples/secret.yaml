---
# REPLACE the token before applying. The same token must be set as
# AUTH_TOKEN on the homerun2-omni-pitcher Deployment.
apiVersion: v1
kind: Secret
metadata:
  name: homerun2-pitcher-auth
  namespace: crossplane-system
type: Opaque
stringData:
  token: REPLACE_ME_WITH_BEARER_TOKEN
